import { createStyles } from "@mantine/core"

const useStyles = createStyles(() => ({
  particles: {
    position: "absolute",
    zIndex: -1,
    height: "99vh",
    width: "99vw"
  }
}))

export default useStyles

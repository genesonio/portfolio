import { createStyles } from "@mantine/core"

const useStyles = createStyles(() => ({
  particles: {
    position: "absolute",
    zIndex: -1,
    height: "100vh",
    width: "100vw"
  }
}))

export default useStyles

import {createStyles, rem} from "@mantine/core"

const useStyles = createStyles(() => ({
  backToTop: {
    width: rem(48),
    height: rem(48),
    position: "fixed",
    right: rem(8),
    bottom: rem(88)
  }
}))

export default useStyles
